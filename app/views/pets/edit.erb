<h1>Update Pet</h1>

<form action="/pets/<%= @pet.id %>" method="POST">
  <label>Name:</label>

  <br></br>

  <input type="text" id="pet_name" name="pet[name]" value="<%= @pet.name%>">

  <br></br>
  Choose an existing owner: <br><br>
  <%@owners.each do |owner|%>
    <input type="checkbox" name="pet[owner_id]" value="<%=owner.id%>" id="<%=owner.id%>"<%='checked' if @pet.owner.include?(owner) %>><%=owner.name%></input>
  <%end%>

  <br></br>

  and/or, create a new owner:
    <br></br>
    <label>Name:</label>
      <input  type="text" id="owner_name" name="owner[name]">
    <br></br>

  <input type="submit" id="Create Pet" value="Create Pet">
</form>
